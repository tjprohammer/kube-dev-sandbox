apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: notifications-route
  namespace: sandbox-app
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: sandbox-gateway
      namespace: sandbox-app
  hostnames:
    - notify.sandbox.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: notifications
          port: 8100
